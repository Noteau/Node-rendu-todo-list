extends ../layout

block content
  h1= title
  p Voici la liste des utilisateurs :
  ul
    each user in users
      li #{user.pseudo} (#{user.firstname} #{user.lastname}) : #{user.email} #{user.password}